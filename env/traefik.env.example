# Cloudflare DNS challenge credentials are read from Docker secrets.
# CF_DNS_API_TOKEN_FILE is the scoped-token method and is all that is required.
CF_DNS_API_TOKEN_FILE=/run/secrets/cloudflare-token
# Only needed if you use a Global API Key instead of a scoped token.
CF_API_EMAIL_FILE=/run/secrets/cloudflare-email
